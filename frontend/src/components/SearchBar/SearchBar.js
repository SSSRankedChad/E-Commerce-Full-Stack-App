import React from 'react';
import { TextField } from '@mui/material';


export default const SearchBar = () => {
  return (
    <TextField
     fullwidth
     label="search"
     className="searchbar"
     />
  );
}
